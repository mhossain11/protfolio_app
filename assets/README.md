# Assets

Recommended production assets:

- `images/profile.jpg` - professional profile image.
- `images/project_*.jpg` - project preview images.
- `icons/` - brand or custom service icons.
- `lottie/` - local loading or scroll animation files.

After adding files, register them in `pubspec.yaml` under `flutter/assets`.
